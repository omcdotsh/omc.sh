export type Xp = {
  title: string;
  company: string;
  date: string;
  description: string;
};

export type Achievement = {
  title: string;
  company?: string;
  date?: string;
  description: string;
};

export type AboutItem = {
  title: string;
};
